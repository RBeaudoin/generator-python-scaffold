language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-python-scaffold' ]; then cd .. && eval "mv $currentfolder generator-python-scaffold" && cd generator-python-scaffold; fi

